Original prompt: 다음으로 — 클래스 라이어의 실제 최소 기능 버전을 독립 앱으로 개발한다.

## 2026-08-19

- 교사 데스크톱·학생 모바일 UI 시안을 생성하고 `docs/designs/`에 저장했다.
- 구현 범위: 방 생성 → 학생 입장 → 역할 확인 → 발언 → 질문 → 투표 → 최종 추리 → 공개.
- 의도적 차이: 이 게임은 움직이는 캔버스 게임이 아니라 대면 대화를 진행하는 DOM 기반 도구다. `render_game_to_text`와 `advanceTime`은 제공하지만 게임 캔버스와 스프라이트는 사용하지 않는다.
- `games/classroom-liar/`에 다른 게임과 분리된 패키지와 Vite 개발 구성을 만들었다.
- 서버 권한 상태 모델을 구현했다. 방 생성, 팀 배정, 비밀 역할, 발언·질문 완료, 비밀 투표·결선, 라이어 최종 추리, 결과 공개, 다음 라운드, 재접속을 포함한다.
- 교사 데스크톱과 학생 모바일 UI를 구현하고 LAN 주소 QR, 연결 상태, 새로고침 복구를 연결했다.
- Socket.IO 모든 명령 payload를 Zod로 검증하고 교사 종료 시 방·역할·투표 데이터를 서버 메모리에서 삭제한다.
- 단위·통합 테스트 14개, typecheck, production build를 통과했다.
- 교사 1개와 학생 5개 Playwright 컨텍스트의 전체 라운드, 교사·학생 새로고침, 종료 흐름을 통과했고 콘솔 오류는 0건이다.
- 시안과 구현을 `docs/designs/classroom-liar-fidelity-ledger.md`에서 비교했다.
- 프로젝트 최상단에 더블클릭용 `Classlation 시작.command`와 `Classlation 종료.command`를 추가했다. 첫 설치·빌드·서버 준비·브라우저 열기·중복 실행 감지·안전 종료를 처리한다.
- 런처 시작/종료, PID 정리, 다른 앱의 4173 포트 보호를 실제 프로세스로 검증했다.
- 당시 TODO: 실제 iPhone·Android 동일 Wi-Fi 확인, 32명 연결 스모크, 첫 교실 파일럿. 이후 첫 수업을 운영해 아래 두 현장 관찰을 얻었다.
- 현장 관찰 1: 학생별 발언·질문 완료 클릭이 대면 대화를 방해했다. 앱은 팀·비밀 정보·투표만 담당하도록 개정한다.
- 현장 관찰 2: 게임 시작과 팀 이동이 겹쳤다. `입장 마감·팀 배정 → 자리 이동 → 교사 게임 시작` 단계를 추가한다.
- `teamSetup`과 `discussion` 상태를 구현하고 기존 `clue`·`question` 클릭 흐름을 제거했다.
- 교사가 입장을 마감해 팀을 먼저 공개하고, 학생들이 자리 이동을 끝낸 뒤에만 비밀 역할을 공개하도록 서버·교사·학생 화면을 개정했다.
- 대면 대화 화면에는 개인별 완료 버튼을 두지 않고, 팀원 한 명 또는 교사가 팀 전체의 투표를 시작하도록 바꿨다.
- 라이어의 최종 추측은 입력창 없이 말로 진행하고 앱은 실제 주제어만 공개한다.
- 타입 검사, 프로덕션 빌드, 단위·통합 테스트 15개를 통과했다.
- 교사 1개·학생 5개 모바일 컨텍스트로 팀 사전 배정, 늦은 입장 차단, 재접속, 대면 대화, 익명 투표, 구두 추측·공개, 종료를 검증했고 콘솔 오류는 0건이다.
- 시각 QA에서 단계 전환 뒤 모바일 스크롤 위치가 남는 문제를 찾아 각 새 단계의 맨 위로 복귀하도록 수정했다.
- TODO: 개정 흐름으로 실제 학생 두 번째 파일럿, 32명 연결 스모크, 실제 Wi-Fi 단절 복구 검증.
- 학생 디자인 감사를 반영해 팀마다 색·기호·별칭이 있는 식별 체계를 추가하고, 역할 공개를 `나만 보는 비밀 카드`로 강화했다.
- 대면 대화 중 투표 시작 버튼을 보조 행동으로 낮춰 `기기를 내려놓고 말하기`가 화면의 주 행동으로 유지되게 했다.
- 중립 결과의 성공 체크 표시를 제거하고, 라이어·실제 주제어를 우선 표시하며 같은 사람의 지목 정보가 중복되지 않도록 정리했다.
- 학생 화면의 작은 안내 글자를 키우고 게임 이름을 `클래스 라이어`로 통일했다.
- 이번 학생 디자인 개선 뒤 타입 검사, 프로덕션 빌드, 단위·통합 테스트 15개를 통과했다.
- 교사 1개·학생 5개 컨텍스트의 전체 라운드를 다시 통과했고, 390×844에서 팀 배정·팀원/라이어 역할·대면 대화·결과 화면을 직접 확인했다. 콘솔 오류는 0건이다.
- 역할 공개 애니메이션의 중간 프레임이 자동 캡처에 잡히는 점을 발견해 0.5초 안정화 후 검사하도록 E2E를 보강했다.
- 결과에 지목 정보가 없는 경우 라이어 칸이 치우치지 않도록 전체 폭으로 정렬했다.
- TODO: 다음 실제 수업에서 팀 색·기호·별칭의 식별 효과와 역할 공개의 흥미도를 관찰한다. 기존 TODO인 32명 연결 스모크와 실제 Wi-Fi 단절 복구 검증도 남아 있다.
- 학생 디자인 개선을 `6bb96c7`로 커밋해 GitHub `main`에 백업했다.
- 두 번째 수업에서 팀 이동·전원 발화·투표 시작 이해도·화면 가독성을 짧게 기록할 수 있는 `docs/classroom-tests/classroom-liar-second-pilot-template.md`를 작성했다.
- 초기 구현 기준에 남아 있던 자동 발언 순서·완료 버튼·온라인 정답 입력 내용을 제거하고 현재 대면 중심 UI 규격과 디자인 감사 기록으로 갱신했다.
- 다음 행동: 실제 수업에서 파일럿 양식을 작성하고 1.0 후보 여부를 판정한다. 새 기능은 그 관찰 결과가 나오기 전까지 추가하지 않는다.

## 2026-08-20

- 두 번째 실제 수업에서 브라우저 이탈 뒤 재입장 실패, 라운드별 팀 변경 요구, 시작 전 수동 팀 조정 요구, 6명 팀 상한 불필요, 닉네임과 실제 호칭 혼선이 확인됐다.
- 재입장은 같은 기기 자동 복구와 교사 승인 요청을 함께 제공하고, 승인 시 기존 학생의 연결 토큰을 교체하는 방향으로 정했다.
- 팀은 기본적으로 유지하되 방 생성 시 `라운드마다 새 팀`을 선택할 수 있게 하고, 각 라운드 시작 전 교사가 팀을 편집할 수 있게 한다.
- 자동 팀 배정의 6명 상한을 제거하고 권장 인원 3~32명을 지원하되, 게임 시작 시 팀당 최소 3명만 검사한다.
- 실명 사용은 프로그램이 강제하지 않고 교사의 시작 안내로 처리한다.
- 구현 기준은 `docs/features/classroom-liar-resilient-teams.md`에 기록했다.
- 학생 재접속 정보를 `localStorage`로 옮겨 같은 브라우저의 새 탭·재실행에서 자동 복귀하도록 하고, 저장 정보가 없을 때는 학생 요청 → 교사 기존 참가자 선택 → 승인·새 토큰 발급 흐름을 구현했다.
- 교사 팀 준비 화면에 학생별 팀 이동, 팀 추가, 빈 팀 제거, 자동 다시 섞기를 추가했다. 한 팀에 6명보다 많이 둘 수 있으며 시작 시 팀당 최소 3명만 검사한다.
- 방 생성 시 `게임 내내 같은 팀`과 `라운드마다 새 팀`을 선택할 수 있고, 새 팀 모드에서는 다음 라운드 전 팀 확인·편집·자리 이동 단계로 돌아가도록 구현했다.
- 타입 검사, 프로덕션 빌드, 단위·통합 테스트 20개를 통과했다.
- 기존 교사 1개·학생 5개 전체 흐름과 교사 1개·학생 10개 복원력 E2E를 모두 통과했다. 후자에서 10명 한 팀, 3·3·4명 수동 편집, 자동 복귀, 교사 승인 재입장, 2라운드 재편성을 검증했고 콘솔 오류는 0건이었다.
- 1440×1024 교사 팀 편집·재입장 요청·2라운드 팀 화면과 390×844 학생 새 팀 화면을 직접 확인했다. 기존 흰 배경·남색 글자·코발트 강조의 디자인 언어를 유지했다.
- 파일럿 양식에 실명 입력 안내, 승인 재입장, 교사 팀 수정, 라운드별 이동 시간을 추가했다.
- 남은 현장 검증: 실제 iPad 브라우저 앱 완전 종료 뒤 자동·승인 복귀와 라운드 재편성 뒤 자리 이동 시간.

## 2026-08-27

- LifeOS의 `Projects/Classlation 이관/`에 들어온 `돋보기 추리 왕.html`, `몸으로 말해요.html`을 다음 독립 게임 후보로 분석했다.
- 두 원본은 단일 HTML과 전역 DOM 상태로 구성되고 Tailwind·아이콘·글꼴·축하 효과를 외부 CDN에 의존한다. 수업 실행 중 외부 요청 0건이 되도록 재구현해야 한다.
- `돋보기 추리 왕`은 교사 화면만으로 의미가 충분하므로 학생 접속과 Socket.IO 없이 `games/magnifier-mystery/`에 먼저 이관한다.
- `몸으로 말해요`는 교사 메인 TV에서 두 팀 주제와 힌트를 함께 확인하고, 시작하면 두 주제를 가린 뒤 두 팀이 각자의 주제로 동시에 진행하는 의도된 구조다. 이 흐름을 `games/classroom-charades/`에 그대로 이관한다.
- 기존 `games/돋보기 추리왕/code_artifact.html`은 LifeOS 원본과 SHA-256이 같은 사용자의 미추적 참고 파일이다. 수정하거나 완성 앱으로 커밋하지 않는다.
- 두 게임이 완성되기 전에는 범용 허브나 공통 게임 엔진을 만들지 않는다. 이후에는 한 게임만 선택 실행하는 가장 작은 루트 실행 장치만 검토한다.
- 상세 규칙·완료 기준·순서는 LifeOS의 `Domains/Development/Plans/Classlation-기존-웹앱-이관-준비.md`에 기록했다.
- 사용자 확인으로 `몸으로 말해요`의 두 팀 주제 동시 공개는 정보 노출 문제가 아니라 의도된 규칙임이 확인됐다. 상대 팀 정답을 알아도 각 팀이 자기 주제로 동시에 진행하므로 게임에 영향을 주지 않는다.
- 두 이관 앱 모두 교사가 메인 TV 한 화면에 띄우는 교사 전용 앱으로 확정했다. `몸으로 말해요`에 팀 기기, 방 코드, 비밀 전송, 재접속을 추가하지 않는다.
- TODO: `magnifier-mystery 이관 → classroom-charades의 두 팀 동시 진행 이관 → 두 앱 메인 TV 실사용 검증`.
- `games/magnifier-mystery/`에 돋보기 추리왕을 React·TypeScript 독립 앱으로 이관했다. 사진 등록·문제 편집·10/18/28% 확대·다른 부분 보기·초성·정답 공개·완료 흐름을 구현했다.
- 돋보기 사진은 서버 전송이나 외부 요청 없이 브라우저 메모리에서만 사용한다. 파일 형식·12MB 크기·디코딩 실패 검사와 임시 URL 해제를 추가했다.
- `games/classroom-charades/`에 몸으로 말해요를 독립 앱으로 이관했다. 두 주제 동시 공개 → 동시 시작 → 공통 타이머 → 팀별 결과 기록 흐름을 보존했다.
- 몸으로 말해요의 다시 뽑기는 기존 두 주제를 목록으로 되돌리며, 팀 이름 반영·시간 종료 상태·점수 보정·기록·주제 목록 편집을 제공한다.
- 두 이관 앱에는 학생 접속, Socket.IO, 방 코드, 비밀 전송을 추가하지 않았다.
- `Classlation 시작.command`에서 세 독립 게임 중 하나를 고를 수 있게 했다. 이 선택기는 설치·빌드·한 포트 실행만 담당하며 공통 게임 엔진은 만들지 않는다.
- 시각 디자인은 흰 배경과 TV 가독성에 필요한 최소 수준만 정리했다. Classlation 전체 브랜드 및 개인 브랜드 에셋이 확정되면 세 게임을 한 번에 조정한다.
- 돋보기 단위 테스트 4개와 전체 흐름 E2E, 몸으로 말해요 단위 테스트 3개와 전체 흐름 E2E를 통과했다.
- 실행 선택기로 클래스 라이어·돋보기 추리왕·몸으로 말해요를 각각 시작하고 `/api/health`로 앱을 확인한 뒤 종료하는 검증을 통과했다.
- 브라우저와 웹 게임 점검 도구로 문제 준비 화면과 두 주제 동시 공개 화면을 확인했다. 수업 중 외부 요청·콘솔 오류가 없고 1440×900 TV 화면에서 가로 넘침이 없었다.
- TODO: 두 앱을 실제 교실 TV에서 운영해 조작 거리·글자 크기·라운드 전환 속도를 관찰하고, 향후 브랜드 시스템 확정 뒤 통합 디자인 감사를 진행한다.
- 실제 화면에서 돋보기 렌즈가 타원으로 찌그러지는 문제가 확인됐다. 원인은 900×620 캔버스에 원형 테두리를 적용한 것이었다.
- 돋보기 표시 캔버스를 800×800 정사각형으로 바꾸고, 원본 사진에서도 정사각형 영역을 잘라 확대하도록 수정했다. 사진 비율과 관계없이 렌즈와 확대 이미지가 찌그러지지 않아야 한다.
- 렌즈의 렌더링 폭·높이가 같은지 검사하는 E2E 단언을 추가했다. 단위 테스트 5개, 타입 검사, 빌드, 전체 게임 흐름, 1440×900 화면·콘솔 검증을 통과했다.
