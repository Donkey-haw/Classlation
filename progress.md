Original prompt: 다음으로 — 클래스 라이어의 실제 최소 기능 버전을 독립 앱으로 개발한다.

## 2026-08-19

- 교사 데스크톱·학생 모바일 UI 시안을 생성하고 `docs/designs/`에 저장했다.
- 구현 범위: 방 생성 → 학생 입장 → 역할 확인 → 발언 → 질문 → 투표 → 최종 추리 → 공개.
- 의도적 차이: 이 게임은 움직이는 캔버스 게임이 아니라 대면 대화를 진행하는 DOM 기반 도구다. `render_game_to_text`와 `advanceTime`은 제공하지만 게임 캔버스와 스프라이트는 사용하지 않는다.
- `games/classroom-liar/`에 다른 게임과 분리된 패키지와 Vite 개발 구성을 만들었다.
- 서버 권한 상태 모델을 구현했다. 방 생성, 팀 배정, 비밀 역할, 발언·질문 완료, 비밀 투표·결선, 라이어 최종 추리, 결과 공개, 다음 라운드, 재접속을 포함한다.
- 교사 데스크톱과 학생 모바일 UI를 구현하고 LAN 주소 QR, 연결 상태, 새로고침 복구를 연결했다.
- Socket.IO 모든 명령 payload를 Zod로 검증하고 교사 종료 시 방·역할·투표 데이터를 서버 메모리에서 삭제한다.
- 단위·통합 테스트 14개, typecheck, production build를 통과했다.
- 교사 1개와 학생 5개 Playwright 컨텍스트의 전체 라운드, 교사·학생 새로고침, 종료 흐름을 통과했고 콘솔 오류는 0건이다.
- 시안과 구현을 `docs/designs/classroom-liar-fidelity-ledger.md`에서 비교했다.
- 프로젝트 최상단에 더블클릭용 `Classlation 시작.command`와 `Classlation 종료.command`를 추가했다. 첫 설치·빌드·서버 준비·브라우저 열기·중복 실행 감지·안전 종료를 처리한다.
- 런처 시작/종료, PID 정리, 다른 앱의 4173 포트 보호를 실제 프로세스로 검증했다.
- 당시 TODO: 실제 iPhone·Android 동일 Wi-Fi 확인, 32명 연결 스모크, 첫 교실 파일럿. 이후 첫 수업을 운영해 아래 두 현장 관찰을 얻었다.
- 현장 관찰 1: 학생별 발언·질문 완료 클릭이 대면 대화를 방해했다. 앱은 팀·비밀 정보·투표만 담당하도록 개정한다.
- 현장 관찰 2: 게임 시작과 팀 이동이 겹쳤다. `입장 마감·팀 배정 → 자리 이동 → 교사 게임 시작` 단계를 추가한다.
- `teamSetup`과 `discussion` 상태를 구현하고 기존 `clue`·`question` 클릭 흐름을 제거했다.
- 교사가 입장을 마감해 팀을 먼저 공개하고, 학생들이 자리 이동을 끝낸 뒤에만 비밀 역할을 공개하도록 서버·교사·학생 화면을 개정했다.
- 대면 대화 화면에는 개인별 완료 버튼을 두지 않고, 팀원 한 명 또는 교사가 팀 전체의 투표를 시작하도록 바꿨다.
- 라이어의 최종 추측은 입력창 없이 말로 진행하고 앱은 실제 주제어만 공개한다.
- 타입 검사, 프로덕션 빌드, 단위·통합 테스트 15개를 통과했다.
- 교사 1개·학생 5개 모바일 컨텍스트로 팀 사전 배정, 늦은 입장 차단, 재접속, 대면 대화, 익명 투표, 구두 추측·공개, 종료를 검증했고 콘솔 오류는 0건이다.
- 시각 QA에서 단계 전환 뒤 모바일 스크롤 위치가 남는 문제를 찾아 각 새 단계의 맨 위로 복귀하도록 수정했다.
- TODO: 개정 흐름으로 실제 학생 두 번째 파일럿, 32명 연결 스모크, 실제 Wi-Fi 단절 복구 검증.
