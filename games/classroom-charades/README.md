# 몸으로 말해요

교사가 메인 TV에 띄우고 두 팀이 서로 다른 주제로 동시에 진행하는 Classlation 게임이다. 준비 화면에서는 두 팀의 정답을 의도적으로 동시에 공개한다.

```bash
pnpm install
pnpm build
pnpm start
```

`http://localhost:4173`을 연다. 학생 기기나 네트워크 연결은 사용하지 않는다.
